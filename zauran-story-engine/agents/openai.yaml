interface:
  display_name: "zauran_story_engine"
  short_description: "Идея → вселенная → герои → сценарий → шот-лист"
  default_prompt: "Use $zauran-story-engine to develop or revise my story at the depth my request needs, preserving my constraints and producing a production package only when requested."
